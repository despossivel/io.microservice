let ObjectId = require('mongodb').ObjectID;

module.exports.run = async (application, dados, socket, jwt) => {



	console.log('rodar resultadoss')
	// socket.join(dados._id); 
	// socket.broadcast.to(dados._id).emit('resultadoRun');



}
